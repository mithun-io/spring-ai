spring:
  application:
    name: chatbot
  ai:
    openai:
      base-url: https://api.groq.com/openai
      api-key: ${GROQ_API_KEY}
      chat:
        options:
          model: llama-3.1-8b-instant
          temperature: 0.7

server:
  port: 80